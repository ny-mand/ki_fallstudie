[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ki_fallstudie"
version = "0.1.0"
description = "Projektmanagement-Tool"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools]
packages = ["src"]
